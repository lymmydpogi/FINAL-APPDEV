/**
 * Copy to apiConfig.override.ts (gitignored) to point at your Symfony server.
 * Physical device on Wi‑Fi cannot use 10.0.2.2 — use your PC's LAN IP.
 */
export const API_BASE_URL = 'http://192.168.1.10:8000';
